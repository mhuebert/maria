machine:
  java:
    version: oraclejdk8
dependencies:
  post:
    - lein npm install
    - script/styles.sh
    - lein cljsbuild once prod
    - node_modules/firebase-tools/firebase deploy --only hosting --token $FIREBASE_TOKEN