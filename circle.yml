machine:
  java:
    version: oraclejdk8
dependencies:
  post:
    - lein npm install
    - script/styles.sh
    - lein cljsbuild once prod
    - firebase deploy --only hosting --token $FIREBASE_TOKEN